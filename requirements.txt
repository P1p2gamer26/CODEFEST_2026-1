# Etapa 1 - Base de conocimiento vectorial (CODEFEST AD ASTRA 2026)
# Solo modelos encoder (no generativos) en todo el pipeline.
# Versiones congeladas del entorno de desarrollo (pip freeze) para reproducibilidad.

# Extraccion
pypdfium2==5.12.1        # extractor de PDF principal (760 PDFs, ~30k paginas)
pdfplumber==0.11.10      # diagnostico de layout; no se usa en la fase offline
trafilatura==2.1.0
pandas==3.0.5
openpyxl==3.1.5
mapbox-vector-tile==2.2.0  # los .pbf del corpus son Mapbox Vector Tiles, no OSM
pytesseract==0.3.13      # requiere ademas el binario tesseract con spa+eng+por
pillow==12.3.0

# Limpieza
langdetect==1.0.9

# Chunking
pysbd==0.3.4
spacy==3.8.14

# Embeddings / FAISS
numpy==2.5.1             # lo importa Entrega/generador.py directamente
sentence-transformers==5.6.1
faiss-cpu==1.14.3
torch==2.13.0            # CPU basta para evaluar: la fase online no entrena nada

# Grafo de conocimiento (bonus)
transformers==5.14.1
networkx==3.6.1

# Informe tecnico (scripts/gen_informe_tecnico.py)
reportlab==5.0.0

# Utilidades / testing
tqdm==4.69.1
pytest==9.1.1

# --- Opcionales: NADA de la entrega los necesita ---------------------------
# Solo los usan dos utilidades de dev/scripts/. Sin ellos, el resto del repo
# (fase offline, fase online, tests y validador) funciona igual.
#   matplotlib -> dev/scripts/ver_grafo.py        (render PNG del grafo)
#   fpdf       -> dev/scripts/gen_synthetic_corpus.py (corpus de prueba)
# matplotlib==3.11.0
# fpdf2==2.9.0

# Modelos de spaCy: NO son paquetes de PyPI, se instalan aparte y solo hacen
# falta para construir el grafo (fase offline). Ver README, seccion 3.3:
#   python -m spacy download es_core_news_sm en_core_web_sm pt_core_news_sm
